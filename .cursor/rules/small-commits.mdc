---
description: Prefer several small git commits per PR instead of one large commit
alwaysApply: true
---

# Small commits

When the user asks to commit or open a PR, split the work into several small commits rather than one large commit covering the whole change.

Each commit should be one concern (content, CI, a single feature, a fix). Do not squash unrelated files into a single commit unless the user asks to.
