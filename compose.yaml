# Local production-shaped run of the same image CI deploys.
#   docker compose up --build
services:
  website:
    build: .
    image: personal-website:local
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
