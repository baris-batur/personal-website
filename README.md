# Personal website

Portfolio for Baris Batur. Next.js 16, typed content in `lib/data.ts`, frozen instrument UI.

```
npm ci
npm run dev
```

Quality gates and the container path are documented in [`deploy/README.md`](deploy/README.md). CI runs them on every push.
